<p align="center"><img src="docs/assets/hero.svg" width="100%"></p>

[English](README.md) | **日本語**

# Brinsfield (2013) — 従業員サイレンスの 6 動機

**Brinsfield (2013)「Employee Silence Motives: Investigation of Dimensionality and Development of Measures」**（*Journal of Organizational Behavior*, 34(5), 671–697; DOI: 10.1002/job.1829）のハイブリッド再現実装である．

- **Track B — 生成的 ABM**（Rust `brinsfield`，[socsim](https://github.com/akitenkrad/rs-social-simulation-tools) ライブラリ上）: Watts–Strogatz 組織ネットワーク上の 6 動機サイレンスシミュレーション．各従業員は `{ineffectual, relational, defensive, diffident, disengaged, deviant}` 上の 6 動機確率ベクトル `MotiveVec6` を持ち，EMA 学習動的で更新される．これにより Brinsfield の横断的な動機分布が定常状態として創発する．排他的な 4 つの意思決定モード `--decision-mode {llm|rule_6dim|rule_4dim|rule_3dim}` が，LLM 駆動の割当を rule ベースの softmax ablation および Knoll 4 類型 / Van Dyne 3 類型への collapse と対比する．
- **Track A — 心理測定的レプリケーション**（Python `brinsfield-tools`）: 独立サンプルでの competing-models CFA（6 因子 vs 1–5 因子 + bifactor）．実調査データなしでも，較正済み合成データ経路でエンドツーエンド実行できる．

## 2 層決定論

LLM 出力は socsim の bit 再現性の **外側** にあるため，設計を 2 層に分ける．

- **決定論的 socsim コア** — 従業員初期化・Watts–Strogatz ネットワーク・スケジューリング・非意思決定 mechanism・3 つの `rule_*` モード．seed 固定で bit 完全再現（`rule_*` は LLM 呼び出し 0 回）．
- **非決定論的 LLM 層** — `voice_decision` のみ．`socsim-llm` の `CachingClient`・`temperature=0`・`(agent_id, t)` 由来 seed で擬似決定論化．プロバイダ順は **Ollama 第一 → OpenAI フォールバック**．

各 run は `llm_meta.json` に決定モード / model / endpoint / 温度 / seed / cache-hit 率を記録する．

## インストールとクイックスタート

```bash
# Rust シミュレーションをビルド（socsim incl. socsim-llm を取得）
cargo build --release

# === rule_6dim ベースライン（LLM 不要） ===
cargo run --release -- run --decision-mode rule_6dim \
    --n-teams 5 --team-size 8 --network-k 6 --network-beta 0.1 \
    --motive-init "0.35,0.20,0.13,0.13,0.13,0.06" \
    --t-max 48 --runs 3 --seed 42

# === LLM モード（Ollama 第一） ===
export OLLAMA_HOST=http://localhost:11434
export OLLAMA_MODEL=llama3.1
cargo run --release -- run --decision-mode llm \
    --llm-cache-path runs/brinsfield_cache.json --t-max 48 --runs 5 --seed 42

# === competing-model ablation ===
cargo run --release -- ablate --decision-modes rule_6dim,rule_4dim,rule_3dim --runs 20

# === Python ツール（合成 Track A；実データ不要） ===
uv sync
uv run brinsfield-tools survey-loader --synthesize-n 300 --sample synth
uv run brinsfield-tools cfa --sample synth          # 6 因子が 1–5 因子に優位
uv run brinsfield-tools reproduce --sample synth    # 一括再現
uv run brinsfield-tools visualize --results-dir results/latest
```

## 再現したアンカー

同梱のスモーク設定で：

- **defensive ≈ 12.65%**: `ablate`/`reproduce` の定常 defensive 比率が Study 1 アンカー近傍（`rule_6dim` ≈ 0.13–0.15）に着地し，ineffectual が最頻（≥ .30），deviant が稀（≤ .08）．
- **6 因子優位**: 合成 CFA で CFI が M1 → M6 へ単調上昇（≈ .34 → 1.00），RMSEA は 0 へ低下．6 因子モデルがすべての 1–5 因子競合に優位（Study 3）．
- **competing-model KL**: `KL(mix‖reference)` は `rule_6dim` < `rule_4dim` < `rule_3dim` の順で最小．6 次元が最もデータに近いという設計仮説と一致．

## ドキュメント

- [アーキテクチャ](docs/architecture.ja.md) — world state，11 mechanism × 6 phase，RNG ストリーム．
- [CLI](docs/cli.ja.md) — `run` / `sweep` / `ablate` / `reproduce` のフラグ．
- [ユースケース](docs/usecases.ja.md) — よくある実験レシピ．
- [可視化](docs/visualization.ja.md) — Python `brinsfield-tools` のプロット．
- [再現](docs/reproduction.ja.md) — Brinsfield Study 1–4 アンカーと出力の対応．

## ライセンス

[MIT](LICENSE)．

---
*This file was generated by Claude Code.*
