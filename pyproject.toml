[project]
name = "brinsfield2013-workspace"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "brinsfield-tools",
]

[tool.uv.workspace]
members = ["tools"]

[tool.uv.sources]
brinsfield-tools = { workspace = true }
